extends /layouts/application.pug

block content
  +formFor({ url: urlFor('session'), method: 'post' })
    +input('email', 'Email address', { autofocus: true, required: true })
    +input('password', 'Password', { type: 'password', required: true })
    button.btn.btn-outline-success.btn-lg.btn-block(type='submit') Sign in
